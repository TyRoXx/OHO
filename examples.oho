if true: print "Hello, world!" print "another"
if false: print "Hallo"
// we don't have else because we don't need it
if true: print "test"

~variable = "123"
print ~variable

~condition = ~variable == "123"
if ~condition: print "it works!"
if !~condition: print "it doesn't work!"

~condition = ~variable == "1234"
if ~condition: print "it doesn't work!"
if !~condition: print "it works!"
